*.log
*.mp4

# Python
__pycache__/
*.pyc
*.egg-info/

# Python venv for scripts/ (see requirements.txt)
.venv/

# colcon build output — regenerated by `colcon build` on every container
# start (see docker-compose.ros.yml); only ros2_ws/src/ is source.
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/

# compiled Hailo model — hardware/model specific, not source (see
# ros2_ws/models/README.md)
ros2_ws/models/*.hef
